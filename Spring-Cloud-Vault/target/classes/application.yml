spring:
  application:
    name: demo
  cloud:
    vault:
      uri: http://127.0.0.1:8200
      token: <TOKEN VALUE FROM VAULT>
      scheme: http
      kv:
        enabled: true
        backend: secret
        default-context: application